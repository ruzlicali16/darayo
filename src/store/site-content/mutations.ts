import { MutationTree } from 'vuex';
import { SiteContentState } from './state';

const mutation: MutationTree<SiteContentState> = {
  someMutation (/* state: SiteContentState */) {
    // your code
  }
};

export default mutation;
